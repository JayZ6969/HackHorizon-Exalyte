import React from 'react'

const classes = {
    container: "flex flex-col items-center justify-center h-screen bg-gray-200",
}

const Login = () => {
  return (
    <div className={classes.container}></div>
  )
}

export default Login